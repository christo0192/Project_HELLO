# Synthetic sentinel used by the secret-bake validator negative control.
#
# This is NOT a real credential. It is a deterministic, unmistakable marker
# that scripts/validate-no-secrets-baked.sh must reject when present in a scan
# target, proving the validator's value-detection rule is non-vacuous.
# Keeping this file inside scripts/__fixtures__/ (outside the Docker build
# context app/voice-livekit) means the normal validation pass stays green.
SYNTHETIC_FORBIDDEN_SECRET_VALUE
